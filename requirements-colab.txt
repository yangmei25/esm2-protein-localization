# Colab supplies its CUDA-compatible PyTorch build; do not replace it.
# The remaining direct dependencies match the verified local environment.
transformers==4.32.1
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.9.0
matplotlib==3.7.2
joblib==1.5.3
tqdm==4.65.0
