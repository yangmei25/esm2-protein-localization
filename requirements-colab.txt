# Colab supplies mutually compatible builds of PyTorch, NumPy, pandas,
# scikit-learn, and Matplotlib. Do not replace that managed GPU stack: current
# Colab runtimes may use a newer Python than the pinned local environment.
transformers>=4.32,<6
joblib>=1.4,<2
tqdm>=4.66,<5
